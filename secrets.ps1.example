# Copy this file to secrets.ps1, then replace the placeholders below.
# secrets.ps1 is ignored by Git so the webhook cannot be committed accidentally.
$env:DISCORD_WEBHOOK_URL = "https://discord.com/api/webhooks/REPLACE_ME"
$env:DISCORD_USER_ID = ""
