name: Movie Ticket Monitor

run-name: Movie Ticket Monitor — ${{ github.event_name }}

on:
  workflow_dispatch:

  schedule:
    # Run at 2, 12, 22, 32, 42 and 52 minutes past every hour.
    - cron: "2,12,22,32,42,52 * * * *"

permissions:
  contents: write

concurrency:
  group: movie-ticket-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Show trigger information
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Schedule: ${{ github.event.schedule }}"
          echo "Branch: ${{ github.ref }}"
          echo "UTC time: $(date -u)"
          echo "Runner time: $(date)"

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: python -m playwright install --with-deps chromium

      - name: Test Discord on manual runs
        if: github.event_name == 'workflow_dispatch'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_USER_ID: ${{ secrets.DISCORD_USER_ID }}
        run: python monitor.py --test-alert

      - name: Run monitor
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_USER_ID: ${{ secrets.DISCORD_USER_ID }}
        run: |
          set +e
          python monitor.py
          code=$?
          echo "MONITOR_EXIT_CODE=$code" >> "$GITHUB_ENV"
          exit 0

      - name: Save monitor state
        run: |
          if git diff --quiet -- state.json; then
            echo "No state changes to commit."
            exit 0
          fi

          git config user.name "movie-ticket-monitor"
          git config user.email "actions@users.noreply.github.com"

          git add state.json
          git commit -m "Update movie monitor state"
          git push

      - name: Fail workflow if every target failed
        if: env.MONITOR_EXIT_CODE == '1'
        run: exit 1
